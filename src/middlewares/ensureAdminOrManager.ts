// src/middlewares/ensureAdminOrManager.ts

import fastify from "fastify";

export const ensureAdminOrManager = (request: any, reply: any, next: any) => {
  // ... Your ensureAdminOrManager middleware logic
};
